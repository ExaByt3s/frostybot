<?php
const EXCHANGE = 'bitmex';          // Any exchange supported by CCXT
const EXCHANGE_API_BASE = '';       // override the exchange api url to use. Sometimes an exchange offers alternative failover urls
                                    // Set to empty string for the default api endpoint

// if using sub-accounts per bot (recommended), leave empty and use subaccount API keys in config.php)
const API_KEY = '';                 // API key
const API_SECRET = '';              // API secret
?>
